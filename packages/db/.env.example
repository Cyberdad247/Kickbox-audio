# Local PostgreSQL connection.
# connection_limit=5 caps the pool to prevent CPU throttling on small servers
# (see verification.md · Database Connection Conservation).
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sovereign?schema=public&connection_limit=5"
